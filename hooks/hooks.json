{
  "description": "Points Claude at the cinematic-web-design skill when a prompt is clearly about building or restyling a web page. Silent on every other prompt, so it costs nothing the rest of the time.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/nudge.mjs\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
